#SET(PROJECT_MODULE_SOURCES
#
#)

IF(DEFINED PROJECT_MODULE_SOURCES)
    SET(PROJECT_SOURCES "${PROJECT_SOURCES} PROJECT_MODULE_SOURCES")

    ADD_LIBRARY(module SHARED ${PROJECT_MODULE_SOURCES})

    SET(PROJECT_LINK_LIBRARIES "${PROJECT_LINK_LIBRARIES} module")
ENDIF()
